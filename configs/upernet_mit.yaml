checkpoint_dir: ./checkpoints/UPerNet/mit_b5
checkpoint_file: upernet_mit_b5-v4
project_name: UPerNet

#----------------------------------#
run_name: mit_b5

architecture: UperNet                # "Unet", "UperNet", "DeepLabV3", "DeepLabV3Plus", "PSPNet", "FPN", "Linknet", "Pan", "MAnet"
encoder_name: mit_b5
encoder_weight: imagenet

seed: 42

batch_size: 4
num_workers: 8

lr: 1e-4

input_size: 512
max_epoch: 5
valid_interval: 1

amp: True

